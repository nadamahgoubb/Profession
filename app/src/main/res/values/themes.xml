<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Profession" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gold</item>
        <item name="colorPrimaryVariant">@color/light_gold</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="android:fontFamily">@font/almarai_regular</item>
        <item name="fontFamily">@font/almarai_regular</item>
        <item name="android:forceDarkAllowed">false</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/header</item>
    <!--    <item name="android:windowTranslucentStatus">true</item>

        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:fitsSystemWindows">false</item>-->
    </style>

        <style name="SpinnerTheme" parent="TextAppearance.AppCompat.Widget.TextView.SpinnerItem">
            <item name="android:drawableEnd">@drawable/spinner_drop_dawn</item>
            <item name="android:hint">"nation"</item>

        </style>
      <style name="SpinnerThemewithoutIcon" parent="TextAppearance.AppCompat.Widget.TextView.SpinnerItem">
             <item name="android:hint">"nation"</item>

        </style>
    <style name="seekpar" parent="Widget.AppCompat.SeekBar">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@android:drawable/progress_horizontal</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_horizontal</item>
        <item name="android:minHeight">20dip</item>
        <item name="android:maxHeight">20dip</item>
         <item name="android:focusable">true</item>
    </style>
    <style name="CustomBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>
    </style>

    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>
    <style name="foodRatingBar" parent="@android:style/Widget.RatingBar">
    <item name="android:progressDrawable">@drawable/rating_bar_full</item>
        <item name="android:minHeight">18dp</item>
        <item name="android:minWidth">@dimen/_140sdp</item>

        <item name="android:numStars">5</item>

        <item name="android:maxHeight">@dimen/_40sdp</item>
    </style>
</resources>